# 0.1.124 - main-branch direct-work rule

User directive:

> directly merge main branch. Add the rules to this project, all the
> other branches are backups before major changes. since you are the
> only worker, you should directly work on main branch for this repo
> unless specified otherwise.

## What changed

- Fast-forwarded `main` to include the previously completed
  `editor-debug-log-archive-goal` work.
- Added a project-specific branch workflow override: agents now work
  directly on `main` unless the owner explicitly names another branch.
- Documented that non-`main` branches in this repository are backup /
  provenance branches before major changes, not active collaboration
  lanes.
- Removed stale `codex` target-branch guidance from the long-form agent
  handoff, prompt recipe, and deployment docs.

## Verification

- `git diff --check` passed after the docs update.

No runtime tests were rerun for this docs-only rule update. The merged
0.1.121-0.1.123 changes keep their original verification notes.

## Files changed

- `docs/AGENTS.md`
- `docs/index.md`
- `docs/deployment/deploy.md`
- `docs/deployment/northflank.md`
- `docs/deployment/overview.md`
- `docs/SUMMARY.md`
- `docs/versions/0.1.124.md`
- `VERSION`
