part of notechondria_frontend;

/// `_logout` moved into the shared `AppShellSessionMixin`
/// (notechondria_shared 0.1.82) as the public `logout()` method.
/// Call sites use `onLogout: logout` — no leading underscore.
/// This file stays as an empty stub so the parts manifest in
/// `lib/main.dart` doesn't break; can be removed in a future
/// round.
