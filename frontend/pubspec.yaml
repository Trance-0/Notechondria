name: frontend
description: Canvas-like calendar client for Notechondria.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  file_selector: ^1.0.3
  flutter_math_fork: ^0.7.4
  flutter_markdown: ^0.7.3+1
  http: ^1.2.1
  markdown: ^7.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
